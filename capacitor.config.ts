import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.worldofelites.accounts',
  appName: 'World of Elites Accounts',
  webDir: 'dist'
};

export default config;
